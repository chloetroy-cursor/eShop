# Cursor skills — Intellias migration demo pack

Order of use: **Scope .NET → Rust** (point at a service → `plan.md`) → implement the first harness-backed slice → **Verify Catalog** (client-style evidence for Catalog.API) → **Migration validate** (Go / No-go / Inconclusive + blast-radius proof). Keep an append-only trail via **Migration decision trail** (`migrations/decisions.tsv`).

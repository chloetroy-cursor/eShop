[package]
name = "ordering"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Ordering domain islands — Rust side of the .NET → Rust demo"
repository.workspace = true

[dependencies]
eshop-core = { workspace = true }

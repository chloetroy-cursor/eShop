[package]
name = "catalog"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Catalog.API domain islands — Rust landing zone for the .NET → Rust demo"
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
